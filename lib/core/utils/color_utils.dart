import 'package:flutter/material.dart';

class ColorUtils {
  ColorUtils._();

  /// The green used on the onboarding background and primary actions.
  static const Color primaryGreen = Color(0xFF007A22);

  /// Blue used for primary CTA buttons (Sign Up, etc.).
  static const Color buttonBlueColor = Color(0xFF0b6cb9);

  /// Error / destructive actions.
  static const Color errorRed = Color(0xFFFF4B4B);
}
